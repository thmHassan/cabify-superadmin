import React from "react";

const CardTitle = ({ title }) => {
  return (
    <h4 className="text-[#252525] font-semibold text-[30px] leading-[41px] mb-[5px]">
      {title}
    </h4>
  );
};

export default CardTitle;
